@model decimal?
@Html.TextBox(
    "",
    Model.HasValue ? String.Format(System.Globalization.CultureInfo.CurrentCulture,"{0:0.00##########}", (decimal)Model) : "",
    new { @class = "text-box single-line" }
)